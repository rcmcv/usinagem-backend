# Copie este arquivo para ".env" APENAS no seu ambiente local de desenvolvimento.
# Em PRODUÇÃO, defina estas variáveis diretamente no provedor (NÃO use .env).

# Ambiente: dev | prod | test
ENV=dev

# Gere uma chave forte (use o comando sugerido no README)
SECRET_KEY=CHANGE_ME

# Em dev você pode começar com SQLite; depois migramos para Postgres
# Exemplo SQLite:
# DATABASE_URL=sqlite:///./dev.db
# Exemplo Postgres:
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname
DATABASE_URL=sqlite:///./dev.db

# JWT
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
JWT_ALG=HS256

# Em dev pode ser ["*"]; em prod, defina domínio(s) do frontend (JSON)
# Ex.: ["https://minhaapp.com","https://www.minhaapp.com"]
CORS_ORIGINS=["*"]
